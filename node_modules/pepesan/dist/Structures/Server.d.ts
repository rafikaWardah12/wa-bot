/// <reference types="node" />
import { WASocket } from '@whiskeysockets/baileys';
import Pepesan from './Pepesan';
import http from 'http';
export default class Server {
    private static instance;
    pepesan?: Pepesan;
    httpServer?: http.Server;
    static init(pepesan: Pepesan): Server;
    static getInstance(): Server;
    getSocket(id: string): WASocket | undefined;
    sendSuccessResponse(res: any, data?: any, message?: string): void;
    sendErrorResponse(res: any, error: any, status?: number): void;
    start(): void;
    private getRoutes;
    getRouter(): import("express-serve-static-core").Router;
    private authMiddleware;
}
