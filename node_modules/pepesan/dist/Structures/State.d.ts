export declare class State {
    clientId: string;
    jid: string;
    private PREFIX_LENGTH;
    private PATH;
    get prefix(): string;
    get dirPath(): string;
    get filePath(): string;
    constructor(clientId: string, jid: string);
    get(): Promise<any>;
    private getFromDb;
    private getFromFile;
    setState(state: string): Promise<void>;
    setFileState(state: string): Promise<void>;
    setDbState(state: string): Promise<void>;
    deleteState(): Promise<void>;
    deleteFileState(): Promise<void>;
    deleteDbState(): Promise<void>;
}
