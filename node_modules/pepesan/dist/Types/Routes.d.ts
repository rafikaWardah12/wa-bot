import { Controller } from "../Structures";
import { Request, Response } from "./Chat";
import { Middleware } from "./Middleware";
export declare type Route = {
    path: string;
    name: string;
    callback?: RouteCallback;
    originCallback?: RouteCallback;
    child?: Route[];
    type: RouteType;
    middlewares?: Middleware[];
};
export declare type RouteType = 'keyword' | 'state' | 'middleware' | 'button' | 'list' | 'menu';
export declare type RouteMap = Map<string, Route>;
export declare type SyncCallback = ((request: Request, ...args: any) => void | Response | Response[]) | (() => void | Response | Response[]);
export declare type AsyncCallback = ((request: Request, ...args: any) => Promise<void | Response | Response[]>) | (() => Promise<void | Response | Response[]>);
export declare type Callback = SyncCallback | AsyncCallback;
export declare type RouteCallback = [typeof Controller, string] | Callback;
